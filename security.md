# BOLD Security Review

审计日期：2026-07-27

本文记录本轮对密码保护、订阅源、派生内容、数据库写入和前端资源加载的复核结果。除特别说明外，攻击者前提均为未登录访客。这里的“受保护”包括全站密码、分类密码、文章或页面自定义字段 `password`，以及正文中的 `{hide}` / `{password:...}` 标记。

当前验证状态：修复已实现，并在 Typecho 1.2.1 + PHP 8.3 + SQLite 的完整本地站点中完成集成验证。当前工作树中的全部 PHP 文件已通过 `php -l`，`assets/js/bold.js` 与 `assets/vendor/view-image.min.js` 已通过 Node.js 语法检查，`git diff --check` 通过；JSON-LD 中的 `</script>` 会编码为 `\u003C\/script\u003E`。四个专项脚本均通过：`tests/helpers_regression.php`、`tests/password_security_regression.php`、`tests/protected_content_regression.php`、`tests/reply_unlock_regression.php`。真实验证覆盖文章与评论 Feed、四类页面密码提交、匿名评论回执、SQLite 阅读量迁移与原子自增、首页隐藏分页，以及桌面和 390x844 移动视口的可见性、暗色模式、FAB 焦点与横向溢出。尚未验证生产 CDN/整页缓存配置及 MySQL、PostgreSQL、读写分离部署。

### 1. RSS/Atom 与派生内容泄露

状态：已修复；Feed 专项回归及 Typecho 1.2.1 真实 Feed 集成验证通过。

深入复核确认：

- Typecho 生成 Feed 条目时会再次匹配文章、分类或索引路由，仅检查 `Typecho_Router::$current === 'feed'` 会失去 Feed 上下文。匿名访客可从主 Feed、分类 Feed 或单篇评论 Feed 读取原本位于密码墙后的内容。
- 评论总 Feed `/feed/comments/` 不经过主题正文模板；受保护文章下的讨论此前可以从该入口公开读取。
- 单篇评论 Feed 的频道级 description 在主题钩子注册前由文章原始摘要生成；即使评论条目已脱敏，内联密码与 `{hide}` 内容仍可能从频道元数据泄露。
- SEO description、Open Graph 图片提取和侧边栏最新评论是正文之外的派生出口。只保护正文模板并不能阻止这些位置读取受保护文本或图片 URL。
- JSON-LD 虽使用 `json_encode`，但没有启用 HTML 十六进制转义；标题或描述中的 `</script>` 仍可提前结束 JSON-LD 元素并注入活动脚本。
- Feed 判定或评论归属查询异常时若返回原内容，会把一次内部错误放大为保密性失效。

本轮修复：

- Feed 保护改为使用在整个 Feed 请求期间稳定的上下文，并覆盖正文、摘要和评论内容钩子。
- `themeInit` 在单篇评论 Feed 输出前重写频道描述：受保护文章整体替换为提示，普通文章也通过共享 fail-closed 解析器剥离保护标记。
- 受保护条目在 Feed 中整体替换为提示；普通条目也会剥离 `{hide}` 和内联密码块，避免标记内的秘密或明文密码进入 XML。
- 受保护文章的评论 Feed 项除正文外还会匿名化作者、邮箱、个人 URL、发布时间与永久链接，避免讨论参与者元数据旁路泄露。
- SEO 描述和社交分享图片在未解锁时不再从受保护正文生成；侧栏最新评论始终排除受保护文章下的评论。
- JSON-LD 使用 `JSON_HEX_TAG | JSON_HEX_AMP | JSON_HEX_APOS | JSON_HEX_QUOT` 编码，脚本闭合序列不能脱离 JSON 字符串上下文；站点标题和 favicon URL 也先按 HTML 属性上下文转义。
- Feed 与派生内容判定采用保密失败策略；内部判定失败时不返回可能受保护的原始内容。
- `tests/protected_content_regression.php` 覆盖普通请求、分类 Atom 路径、评论总 Feed 路径和已初始化 Feed 对象的上下文识别。

残余边界：

- 文章标题、发布时间和永久链接仍是公开元数据，不应在标题或 URL 中放置秘密。
- 管理员可关闭 Feed 整体脱敏；这是显式降低保护级别的配置，不应在包含秘密内容的站点启用。
- 升级前已被阅读器、搜索引擎或 CDN 获取的副本无法由主题撤回，部署后应清理可控缓存。

### 2. 保护标记和模板门禁失效

状态：已修复；保护标记专项回归通过。

深入复核确认：

- 基于非嵌套正则的 `{hide}` / `{password:...}` 处理会在嵌套标记时留下外层尾部；未闭合的内联密码块还可能直接返回原文，作者的格式错误因此会导致全文泄露。
- 没有内联密码块时调用内联处理器会误删 `{hide}`，破坏“评论后可见”的授权流程。
- 匿名 `{hide}` 解锁此前只检查客户端可任意改写的 `__typecho_remember_mail`，再按邮箱查找任意已审核评论。攻击者一旦猜到或从 Gravatar 哈希还原评论邮箱，无需实际提交对应评论即可冒用授权。
- 时间轴和友情链接自定义页面模板直接渲染页面内容，没有执行与默认 `page.php` 相同的页面级密码门禁。
- 列表摘要只识别全站和分类密码，遗漏单篇 `fields->password`；首页、标签、搜索或作者列表因此可能公开单篇受保护内容。

本轮修复：

- 保护标记由唯一的共享解析器处理，支持嵌套，并在未知、错序或未闭合状态下从对应开启位置停止公开输出。
- `{hide}` 与内联密码的授权流程保持独立；没有内联密码时不再改写评论后可见标记。
- 匿名评论成功写入后由 `Widget_Feedback::finishComment` 签发 HttpOnly、SameSite 的服务器 HMAC 回执，绑定 `cid + coid + 规范化邮箱`。解锁时必须同时验证回执，并查询这条具体评论仍为 `approved`；伪造记忆邮箱、篡改回执、跨文章复用、邮箱不匹配及借用同邮箱历史评论均失败。
- 默认页面、时间轴和友情链接模板统一执行页面级密码门禁。
- 列表、Feed、SEO 和侧栏复用统一的保护判定，包含单篇文章或页面自定义字段 `password`。
- `tests/protected_content_regression.php` 覆盖同类/跨类型嵌套、未闭合、错序关闭、孤立关闭、畸形标记、空密码及仅含 `{hide}` 的流程隔离。
- `tests/reply_unlock_regression.php` 覆盖伪造记忆邮箱拒绝、合法签名与精确已审评论接受、签名篡改、跨文章、邮箱不匹配、待审状态和同邮箱历史评论隔离。

残余边界：

- 密码保护是应用层访问控制，不是数据库静态加密；拥有数据库、备份或 Typecho 管理权限的人仍可读取原文和主题密码配置。
- 修复按保密优先处理损坏标记，格式错误可能隐藏比作者预期更多的后续公开内容，但不会因此公开保护块。
- 升级前的匿名评论者没有新签名回执，需要重新提交一次评论；回执 7 天到期，审核超过有效期时也需要重新提交。登录用户仍按已审核评论的 `authorId` 判断。

### 3. 可伪造解锁票据与过宽权限绕过

状态：已修复；密码票据专项回归通过。

深入复核确认：

- 旧 HMAC 密钥由公开的 `siteUrl` 派生，攻击者一旦知道候选密码即可离线构造合法票据；“未配置密码”的回退值也可由公开信息计算。
- 旧逻辑允许任意已登录账号跳过页面级密码，订阅者等低权限用户因此获得了未授予的读取能力。
- 裸 SHA-256 Cookie 没有过期时间，可被永久重放。原生 `header('Location: ...')` 还可能被 Typecho 后续响应处理覆盖成 `200 OK`，造成提交重放和行为不一致。

本轮修复：

- 解锁票据使用 Typecho 随机站点密钥签名，并绑定绝对过期时间和当前所需密码；票据默认 7 天失效，密码或站点密钥变化会立即使旧票据失效。
- 页面级免密权限收紧为 `editor` 及以上，分类、文章、页面和列表判断使用相同边界。
- 密码比较使用 `hash_equals`，失败路径增加随机延迟；Cookie 名称经过清理并为非 ASCII 分类 slug 加入哈希隔离。
- 成功提交通过 Typecho Response API 重定向；Cookie 属性与路径由统一辅助函数写入。
- `tests/password_security_regression.php` 覆盖私密签名密钥、`v2` 格式、正确/错误密码、签名篡改、过期与超长有效期拒绝、旧公开密钥票据拒绝、缺失站点密钥失败、subscriber/editor 权限边界、列表单篇密码识别和 Cookie 写入同步。

残余边界：

- 主题没有提供持久化的 IP/账号级速率限制。随机失败延迟只能抬高在线猜测成本，公开站点仍应在反向代理或 WAF 配置限速，并使用高熵密码。
- 必须使用 HTTPS，否则密码提交和 Cookie 仍可能在传输链路被窃取。
- 密码保护依赖 Typecho 1.2.1 安装时生成的随机站点 `secret`；缺失或为空时主题拒绝签发解锁票据，需要先修复站点配置。
- 轮换 Typecho 站点密钥、修改密码或本次升级都会要求访客重新解锁，这是预期行为。

### 4. 数据库写入、时间轴链接与查询钩子兼容性

状态：已修复；辅助函数专项回归及 Typecho 1.2.1 + SQLite 集成验证通过。

深入复核确认：

- 公开文章请求曾在缺少 `views` 列时尝试执行 DDL；匿名流量可反复触发高权限结构变更和异常路径。
- 原始 DDL/UPDATE 默认使用读连接，读写分离部署可能失败或写入只读副本；`INT(10)` 也不是跨数据库可移植的列定义。
- 时间轴固定链接只使用关联查询返回的任意分类，忽略 Typecho 的 `order` / `mid` 主分类排序、父级目录和 URL 编码，层级分类、多分类或非 ASCII slug 可能生成 404 链接。
- 首页 `Widget_Archive::query` 钩子会接管核心取数。直接覆盖已有插件回调或重复 `fetchAll()` 会造成插件冲突、重复文章或分页异常。

本轮修复：

- 公开请求不再自动执行建列 DDL；浏览量仅在列存在时读取和原子更新，写入显式使用写连接并对数据库异常做降级处理。
- `views` 列迁移只在管理员打开主题设置页时执行，使用跨数据库的 `INTEGER DEFAULT 0` 和 WRITE 连接；并发或无权限失败后重新探测，前台始终只读降级。
- 时间轴按核心规则选择主分类，批量还原完整父级目录，并对 slug、category 和 directory 参数 URL 编码；循环分类树保守退回主分类。
- 首页隐藏加密分类仅在选项启用且确有受保护分类时注册：早期钩子用带独立别名的 `LEFT JOIN ... IS NULL` 同步过滤列表 SQL 与计数 SQL，避免 Typecho 查询构造器错误引用手写子查询关键字；末尾钩子仅在其他插件尚未取数时补做 `fetchAll()`。
- `tests/helpers_regression.php` 覆盖分类排序、正常父链、缺失父级、循环和无分类场景。

残余边界：

- 新安装或旧表缺少 `views` 列时，管理员需打开一次主题设置页触发迁移；数据库账号没有 DDL 权限时该页面会继续重试，而公开页面只显示 `0` 并跳过写入。
- 已在 SQLite 验证缺列时前台只读降级、后台迁移建列、首次原子自增和同一浏览器不重复计数；尚未在 MySQL、PostgreSQL 和读写分离环境验证，部署前仍应在与生产相同的数据库驱动上测试。
- 极端情况下，自定义插件使用更早或更晚的非标准 query 权重仍可能越过主题的前后钩子，应在安装查询类插件后复测首页。

### 5. 前端可用性与第三方资源边界

状态：已修复；PHP/JavaScript 静态检查与本地真实站点浏览器回归通过。

深入复核确认：

- 防闪烁门控原先无条件隐藏 `body`，但解除计时器注册在同步 Tailwind CDN 之后。CDN 下载缓慢或失败时，解析器尚未创建 `body`，页面可能长时间保持空白。
- Prism 页头 CSS 只使用原文启发式，页脚 JS 还使用渲染后探测；缩进代码块等路径会加载 JS 却遗漏配套 CSS。
- ViewImage 来自个人第三方站点，外部资源被替换或不可用会影响所有文章页。

本轮修复：

- 加载门控状态移到 `<html>`，解除函数即使后续资源失败也能清除状态；Tailwind 与 typography 使用固定版本离线编译，页面只加载本地 `assets/css/tailwind.min.css`，不再运行浏览器端编译器。
- Prism CSS 在文章和独立页加载，JS 继续按原文或渲染结果中的代码块条件加载，两个探测路径都有样式可用。
- MathJax 探测改为识别成对显示公式、LaTeX 定界符及含数学信号的行内公式，普通货币金额和 `$9 to $29` 不再误加载。
- ViewImage 改为主题内自托管；其余版本固定的第三方脚本使用 SRI，并按页面类型或内容条件加载。

残余边界：

- Google Fonts、Prism、tocbot、MathJax、Mermaid 和 Turnstile 仍依赖各自 CDN。SRI 降低资源被篡改风险，但不能解决网络可用性；Google Fonts 可在主题设置中关闭。
- `customHead` / `customFooter` 是管理员信任边界，注入其中的脚本不受主题的 SRI 或行为约束。

## 验证记录与部署验收

本轮本地真实站点已验证：

1. 主 RSS2、RSS1、Atom、分类 Feed 与评论总 Feed 不含测试秘密；受保护评论项的正文和参与者元数据均已脱敏。
2. 分类、文章自定义字段、时间轴页面和友情链接页面四类密码门禁均完成错误密码、正确密码 `302`、Cookie 属性及解锁后访问验证。
3. 匿名评论真实 POST 会签发文章级回执；已有 approved 评论后仅伪造 `__typecho_remember_mail` 仍保持锁定，携带签名回执才解锁。
4. 首页隐藏在每页 1 篇时只返回 3 篇公开文章，总页数为 3，第 4 页为 404；SQLite 阅读量缺列降级、后台迁移和原子自增均通过。
5. 浏览器在桌面及 390x844 移动视口下确认本地 Tailwind CSS 已加载、Play CDN 请求为零，并通过防闪烁兜底、暗色模式、FAB 折叠焦点、横向溢出和元素重叠检查。

生产发布仍应在与线上相同的数据库、反向代理和 CDN 环境复测缓存响应头与分页；启用 Turnstile 后还应验证成功、失败、过期令牌和验证服务不可用路径。阻断剩余第三方资源时应确认页面在 1.5 秒后可见，并评估对应增强功能不可用时的降级体验。
